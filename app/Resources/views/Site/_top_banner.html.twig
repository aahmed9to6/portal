<section class="clearfix homeBanner" style="background-image: url(/site-assets/img/banner/banner1.jpg);">
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <div class="banerInfo">
                    <h1>{{ 'Learn, Share, and Repeat'|trans(domain = 'banner') }}</h1>
                    <p>{{ 'We will help to find out great things around you'|trans(domain = 'banner') }}</p>
                    <form class="form-inline" action="">
                        <div class="form-group">
                            <div class="input-group">
                                <div class="input-group-addon">{{ 'Find'|trans(domain = 'banner') }}</div>
                                <input type="text" class="form-control" id="findItem" placeholder="{{ 'What are you looking for?'|trans(domain = 'banner') }}">
                                <div class="input-group-addon addon-right"></div>
                            </div>
                        </div>
                        <button type="submit" class="btn btn-primary">{{ 'Search'|trans(domain = 'banner') }} <i class="fa fa-search" aria-hidden="true"></i></button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</section>